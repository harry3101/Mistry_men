# Leave empty in local dev — Vite proxies /api to http://localhost:5000
# On Render, set automatically via render.yaml (fromService → mistry-backend)
# VITE_API_URL=https://mistry-backend.onrender.com
